{
  "name": "stac-lookup-backend",
  "version": "1.0.0",
  "type": "module",

  "dependencies": {
    "express": "^4.18.0",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "helmet": "^7.1.0",
    "compression": "^1.7.4",
    "dotenv": "^16.3.0",
    "@turf/simplify": "^6.5.0",
    "@turf/helpers": "^6.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.0"
  },
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "lint": "eslint src --ext js --report-unused-disable-directives --max-warnings 0",
    "test": "jest",
    "test:watch": "jest --watch"
  }
}